export { auth } from '@/firebase/auth';
export { firebaseApp } from '@/firebase/config';
export { COLLECTIONS, firestore } from '@/firebase/firestore';
export { getRealtimeDatabase, REALTIME_PATHS } from '@/firebase/realtimeDatabase';
export { storage, STORAGE_PATHS } from '@/firebase/storage';
